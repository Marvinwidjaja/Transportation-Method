#ifndef FORMTABLE_H
#define FORMTABLE_H

#include <QMainWindow>

class formtable : public QMainWindow
{
    Q_OBJECT
public:
    explicit formtable(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // FORMTABLE_H
